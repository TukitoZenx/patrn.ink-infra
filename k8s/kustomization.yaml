apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: patrn-ink

resources:
  # Base resources (namespace, secrets)
  - base/

  # Applications
  - apps/api/
  - apps/ui/

  # Shared infrastructure
  - ingress.yaml
  - pdb.yaml

commonLabels:
  app.kubernetes.io/part-of: patrn-ink
  app.kubernetes.io/managed-by: kustomize
