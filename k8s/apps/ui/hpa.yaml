apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: patrn-ui
  namespace: patrn-ink
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: patrn-ui
  # Reduced for k3s single-node setup (increase if adding agents)
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
