apiVersion: v1
kind: ConfigMap
metadata:
  name: patrn-ui-config
  namespace: patrn-ink
data:
  # UI environment variables
  # Adjust based on your Next.js/Vite setup
  NODE_ENV: "production"
  NEXT_PUBLIC_API_URL: "https://patrn.ink"
  NEXT_PUBLIC_BASE_URL: "https://patrn.ink"
